# Harness durable layer — each project instance generates its own data.
harness.db
harness.db-wal
harness.db-shm

# Rust build output for harness tooling.
target/
dist/

# Downloaded Harness CLI binary for installed project instances.
scripts/bin/harness-cli

# .NET build output — never commit compiled artifacts.
[Bb]in/
[Oo]bj/
*.dll
*.pdb
*.user

# Node / Next.js frontend build output.
node_modules/
.next/
out/
.vercel/

# Environment files.
.env
.env.local
.env.*.local
